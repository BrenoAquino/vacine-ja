health-check:
  handler: src/dunctions/health_check/main.handler
  memorySize: 256
  timeout: 60
  events:
    - schedule: cron(0 0 ? * * *)
    - schedule: cron(0 8 ? * * *)
    - schedule: cron(0 19 ? * * *)